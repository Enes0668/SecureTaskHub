version: '3.8'
services:
  db:
    image: postgres:latest
    container_name: secure_task_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: SecureTaskDb
    ports:
      - "5432:5432"